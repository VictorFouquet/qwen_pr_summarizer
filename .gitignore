# Secrets — never commit a token or env file
.env
.env.*
# ...but do track the template
!.env.example

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Research outputs (generated). Force-add log.jsonl if you want to version a run.
research/log.jsonl
research/progress.html
