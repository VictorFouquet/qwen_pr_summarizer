# Secrets — never commit a token or env file
.env
.env.*
# ...but do track the template
!.env.example

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# research/log.jsonl and research/progress.html are intentionally TRACKED: the log is
# the experiment record (every trial, note, and hypothesis) and the dashboard is its
# rendered snapshot. They are the decision trail — versioned on purpose.
